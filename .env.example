# Local Postgres host port. If 5432 is already in use (e.g. a native Postgres),
# set DB_PORT to a free port (e.g. 5433) here AND in DATABASE_URL below.
DB_PORT=5432
DATABASE_URL=postgres://clockchain:clockchain@localhost:5432/clockchain
PORT=8080
RUST_LOG=info
